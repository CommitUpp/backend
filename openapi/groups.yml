get:
  summary: 所属グループ一覧取得API
  operationId: getMyGroups
  security:
    - SupabaseAuth: []
  responses:
    '200':
      description: 所属グループ一覧取得成功
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/GetMyGroupsResponse'
    '401':
      description: 認証エラー
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/UnauthorizedError'
    '500':
      description: サーバー内部エラー
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/InternalServerError'

post:
  summary: グループ作成API
  operationId: createGroup
  security:
    - SupabaseAuth: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/CreateGroupRequest'
  responses:
    '201':
      description: グループ作成成功
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/CreateGroupResponse'
    '400':
      description: リクエスト不正（バリデーションエラーなど）
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/BadRequestError'
    '401':
      description: 認証エラー
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/UnauthorizedError'
    '500':
      description: サーバー内部エラー
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/InternalServerError'
