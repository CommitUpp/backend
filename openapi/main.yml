openapi: 3.0.3
info:
  title: CommitUpp API
  version: 1.0.0

servers:
  - url: /api/v1

paths:
  /user/callback:
    $ref: './user-login-callback.yml'
  
components:
  securitySchemes:
    SupabaseAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    LoginCallbackRequest:
      type: object
      required:
        - access_token
      properties:
        access_token:
          type: string

    LoginCallbackResponse:
      type: object
      required:
        - status
      properties:
        status:
          type: string

    BadRequestError:
      type: object
      required:
        - message
      properties:
        message:
          type: string

    UnauthorizedError:
      type: object
      required:
        - message
      properties:
        message:
          type: string

    InternalServerError:
      type: object
      required:
        - message
      properties:
        message:
          type: string